import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.prim_lek3',
  appName: 'prim_lek3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
